'use client'
import{useState}from'react'
export default function Checkout(){const[done,setDone]=useState(false);if(done)return <div className="page error-state"><span className="eyebrow">ORDER PREVIEW</span><h1>Thank you.</h1><p>This frontend checkout stops before payment. No order has been placed.</p></div>;return <div className="page"><header className="page-hero"><span className="eyebrow">SECURE CHECKOUT PREVIEW</span><h1>Where should the cart go?</h1><p>This UI shell does not collect payment or create a real order.</p></header><form className="form-grid panel" onSubmit={e=>{e.preventDefault();setDone(true)}}><input className="field" required placeholder="Full name"/><input className="field" type="email" required placeholder="Email"/><input className="field" required placeholder="Phone"/><input className="field" required placeholder="Delivery address"/><input className="field" required placeholder="Bengaluru PIN code"/><button className="button">Review frontend order</button></form></div>}
